export enum cascadeActions {
  SHARD_COUNT = 'shard_count',
  USER_GET_BY_ID = 'user:get_by_id',
  USER_GET_MUTUAL_GUILDS = 'user:mutual_guilds',
}

export enum cascadeErrors {
  INVALID_SHARD = 'invalid_shard',
  USER_NOT_FOUND = 'user_not_found',
}
