"""
Active login sessions
"""
type Session {
  id: ID!
}

"""
Standard user object
"""
type User {
  id: ID!
  discordId: String!
  sessions: [Session!]
}

type Query {
  """
  Get currently authenticated user
  """
  me: User
}
